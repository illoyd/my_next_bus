- flash.each do |name, msg|
  = content_tag :div, class: "flash-#{ name } fade in", role: 'alert' do
    %button.close{"data-dismiss" => "alert", type: "button"}
      %span{"aria-hidden" => "true"} &times;
      %span.sr-only Close
    = msg
